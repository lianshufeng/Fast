#本地端口
server.port=${BreakerDashboard.port}

#开启支持的端点
management.endpoints.web.exposure.include=hystrix


turbine.appConfig=*
turbine.aggregator.clusterConfig= default
turbine.clusterNameExpression= new String("default")